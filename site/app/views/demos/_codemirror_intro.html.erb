<p>
  Shared state is a <code>Y.Text</code>, bound to CodeMirror 6 with
  <code>y-codemirror.next</code>. Remote cursors and selections come from
  awareness. The server has no idea this is code; it syncs the text.
</p>
