<%# Above the grid on purpose: below it, adding a row moves the button. %>
<div id="toolbar"></div>
<table id="grid" data-document-key="<%= @document_key %>">
  <thead></thead>
  <tbody></tbody>
</table>
