<p>
  Shared state is a <code>Y.Array</code> of card <code>Y.Map</code>s. Add a
  card, drag it between columns, delete it — across two windows. A move is one
  <code>map.set</code>, so two people moving different cards never conflict.
</p>
