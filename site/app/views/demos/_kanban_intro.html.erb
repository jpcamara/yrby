<p>
  Shared state is a <code>Y.Array</code> of card <code>Y.Map</code>s. Add a
  card, drag it between columns, delete it, in two windows at once. A move
  is a single <code>map.set</code>, so two people moving different cards
  don't conflict.
</p>
