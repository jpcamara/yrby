Description:
    Creates the migration for the gem's document storage (Y::Document +
    Y::DocumentUpdate). The models and the Y::DocumentChannel that syncs
    through them ship in the gem; nothing else lands in the app.

Example:
    bin/rails generate yrby:install

    This will create:
        db/migrate/XXXXXXXX_create_y_tables.rb
